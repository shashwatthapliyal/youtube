import React from 'react'

const ButtonItems = ({ name }) => {

  return (
    <div>
      <button className=' px-7 bg-gray-300 rounded-lg'>{name}</button>
    </div>
  )
}

export default ButtonItems